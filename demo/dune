(install
 (files
  (run-server.sh as belenios-start-server)
  (stop-server.sh as belenios-stop-server))
 (section bin)
 (package belenios-server))

(install
 (files mime.types)
 (section share)
 (package belenios-server))
