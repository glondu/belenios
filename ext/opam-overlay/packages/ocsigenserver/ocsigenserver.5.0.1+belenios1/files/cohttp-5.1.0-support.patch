--- ocsigenserver.orig/src/server/ocsigen_cohttp.ml
+++ ocsigenserver/src/server/ocsigen_cohttp.ml
@@ -28,11 +28,8 @@
     Cohttp.(Code.string_of_method (Request.meth request));
 
   Cohttp.Header.iter
-    (fun key values ->
-       (print_list
-          (fun fmt value -> Format.fprintf fmt "\t%s = %s\n" key value)
-          fmt
-          values))
+    (fun key value ->
+       Format.fprintf fmt "\t%s = %s\n" key value)
     (Cohttp.Request.headers request)
 
 let connections = Hashtbl.create 256
